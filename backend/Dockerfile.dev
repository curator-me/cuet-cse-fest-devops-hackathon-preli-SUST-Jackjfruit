FROM node:18-alpine

WORKDIR /app

# Install dependencies first (caching layer)
COPY package*.json ./
RUN npm install

# Copy source code
COPY . .

# Use the port defined in the problem
EXPOSE 3847 
# (Note: Use 5921 for Gateway)

# Start command for dev
CMD ["npm", "run", "dev"]
